# Python
__pycache__/
*.py[cod]
*.egg-info/
build/
dist/
.eggs/

# Environments
.venv/
venv/

# Testing
.pytest_cache/
.coverage
htmlcov/

# Rendered example figures are regenerated by the test suite, wherever they land
# (the examples write relative to the caller's working directory).
# tests/baseline/ is NOT ignored — those references are the comparison gate.
outputs/
tests/outputs/

# Editors / OS / tooling
.DS_Store
.ipynb_checkpoints/
.vscode/
.idea/
.claude/

# A stale snapshot of an older revision of py_m_map/, kept locally but not part
# of the source of truth.
Py_M_Map.zip
